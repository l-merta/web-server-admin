//import { useState } from 'react'

function App() {
  const apiUrl = import.meta.env.VITE_API_URL;
  console.log(apiUrl);

  return (
    <>
      React + Express + Docker boilerplate
    </>
  )
}

export default App
